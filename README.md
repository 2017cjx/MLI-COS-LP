# MLI-COS-LP

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal in your browser (typically http://localhost:5173/ or http://localhost:3001/ if port 3000 is in use).
